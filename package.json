{
  "name": "ittybittybites-platform",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/validate-content.mjs && node scripts/generate-content.mjs && tsc && vite build && cp dist/index.html dist/404.html",
    "validate": "node scripts/validate-content.mjs",
    "generate": "node scripts/generate-content.mjs",
    "preview": "vite preview",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "marked": "^13.0.3",
    "typescript": "^5.5.4",
    "vite": "^5.4.2",
    "vite-plugin-pwa": "^0.20.1"
  },
  "engines": { "node": ">=20" }
}
